#ifndef DEF_TAS
#define DEF_TAS

	int parent(int _index);
	int gauche(int _index);
	int droite(int _index);

	/*
	void entasserMax(Tas * _tas, int _index);
	void construireTasMax(Tas * _tas);
	*/

#endif